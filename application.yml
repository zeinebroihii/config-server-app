eureka:
  client:
    healthcheck:
      enabled: true
    service-url:
      defaultZone: http://discovery:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh
  endpoint:
    health:
      show-details: always